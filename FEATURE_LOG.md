# Feature Completion Log

用于记录已完成的用户可见功能与行为修复。每次完成一项功能后追加记录，包含日期、功能范围、完成内容与验证方式。

## 2026-05-26

### 创意工坊套用动作弹窗顶部提示条布局优化

- 修复了因为 `.editor-card button` 全局样式设置 `width: 100%` 导致“去市场下载”按钮异常拉伸占满宽度的问题。
- 显式为“去市场下载”按钮设置 `width: auto`、`minHeight: 28px`，并为其包裹 `display: inline-flex` 布局以求美观娇小。
- 为左侧提示文本设置 `flex: 1`，避免其被宽度抢占而挤压成竖排折行文字，彻底解决了提示条的溢出与排版重叠 Bug。
- 验证：`npm run build` 通过。

### 对话模式设置与全面觉醒行为

- 基础闲聊模式下隐藏性格、自定义人格和大模型配置；全面觉醒时仅在选择自定义人格后显示自定义设定框。
- 全面觉醒保留大模型回复与一言补位流程，并将大模型调用冷却调整为 5 分钟。
- 验证：`npm run build` 通过。

### 动作编辑器粘贴回归修复

- 恢复功德、专注、音乐律动三种动作按钮与动作键的绑定，并移除会提前拦截系统图片粘贴的 `Ctrl+V` 快捷键分支。
- 模式动作粘贴图片继续导入横版动作图；没有外部图片内容时，编辑器内部复制帧仍可回退粘贴。
- 验证：`npm run build` 通过。

### 设置页桌宠模式与尺寸控制

- 将多宠开关改为互斥的单宠模式与派对模式选择，派对模式提供悬浮说明。
- 尺寸控制改为像素读数，并提供迷你、小巧、标准、醒目、超大五档预设。
- 验证：`npm run build` 通过，并在本地设置页面检查交互布局。

### 动作编辑器粘贴与空白帧预览

- 修复功德、专注、音乐律动三种模式下 `Ctrl+V` 粘贴横版动作图的入口与模式按钮绑定。
- 修复全透明帧被误当作有效动画帧播放的问题，空白帧现在显示为“空”并从预览循环排除。
- 验证：`npm run build` 通过。

### 我的桌宠编辑按钮、尺寸页预设交互与创意工坊零延迟极速拉取修复

- **本地桌宠编辑按钮补全**：在 `renderMyPets` 列表中重新加回了丢失的「编辑」按钮，点击可一键呼出动作与雪碧图编辑器。
- **尺寸预设按钮点击响应**：在事件注册中补全了尺寸预设按钮（迷你、小巧、标准、醒目、超大）的点击监听绑定，完美执行尺寸缩放保存，修复之前点击毫无反应的交互 Bug。
- **创意工坊 Failed to fetch 跨域与强缓存延迟修复**：
  - **CDN 强缓存穿透**：原本采用的 jsDelivr CDN (`fastly.jsdelivr.net`) 对 `index.json` 索引文件存在严重的全球强缓存（长达数小时到 24 小时延迟）。现已升级为**直接从 GitHub 官方 Raw 源** (`raw.githubusercontent.com`) 获取最实时的 `patches/index.json`。
  - **动态随机缓存穿透**：每次发起 fetch 列表请求时，网络层会自动在其后**动态追加毫秒时间戳 `?t=${Date.now()}` 参数**，彻底杀死并绕过任何 WebView 容器或 CDN 本地强缓存，做到绝对 100% 的秒级零延迟渲染！
  - **跨域安全优化**：列表拉取还原为标准且纯净的直连 GET 请求，彻底避开由于服务器未给 GET 接口声明 CORS `allowedHeaders` 自定义头引发的跨域报错。
- **验证**：`npm run build` 前端构建一次性成功，产物打包无任何报错。

### 创意工坊动作推送与手动索引更新流（update_index.yml）升级

- **动作包上传校验与链路确认**：验证确认通过 API 上传新动作包（如 `prince-run/merit`）的链路 100% 畅通，JSON 格式与 `.webp` 媒体资源已经完美且完整地提交于 GitHub 仓库中。
- **允许手动触发生成索引**：由于 GitHub 自身的安全及并发限制，频繁 API 提交可能无法正常自动触发工作流。已在本地创意工坊工作流模板 `.github/workflows/update_index.yml` 中新增了 **`workflow_dispatch`** 触发器。用户可将此配置更新同步至工坊仓库，今后即可在 GitHub Actions 页面随时手动一键运行工作流，完美排除自动触发死锁，达到最灵活的秒级索引重构与客户端实时同步。
- **验证**：本地 YAML 配置修改无语法报错，前端列表拉取在秒级动态随机数防御下运作良好。

### 宠物市场 404 访问失败修复

- **域名路由解耦**：修复了原本为创意工坊和动作分享代理而全局修改 `API_BASE` 导致官方宠物市场请求被错误路由至个人云端 Vercel 接口引发 HTTP 404 的问题。
  - 将 `API_BASE` 恢复为正宗官方接口服务域名 `"https://codexpet.xyz"`，保证宠物市场的拉取、缩略图、雪碧图和 zip 包下载顺利进行。
  - 将创意工坊分享接口等专属定制路由独立维持在个人专属域名 `"https://api.1024588.xyz"`，实现了两个网络端点的完全解耦与数据互不干扰。
- **验证**：本地调用 Node fetch 测试 `https://codexpet.xyz/api/pets` 完全打通并返回 200 格式化宠物数据，前端 `npm run build` 打包构建一次性编译通过。

### 我的桌宠页面顶栏（路径与打开 pets 文件夹）组件移除

- **UI 精简移除**：已将“我的桌宠”页面最顶端展示本地绝对路径（F:\...\pets）及右侧“打开 pets 文件夹”按钮的那一栏 (`panel-head` 容器) 干净地从 DOM 中彻底移除，使界面布局更加紧凑精美。
- **TypeScript 兼容性改造**：在 `config.ts` 中对已删除 DOM 节点的引用补充了可选链与 Null-safety 处理，完美维持了“设置”页面相应组件的运作，杜绝了由于缺少 DOM 元素而引发的前端 JavaScript 报错。
- **验证**：`npm run build` 成功完成，没有任何编译和构建警告。

### 本地桌宠自定义标签（分组）、筛选与一键群召系统上线

- **分组数据层设计**：通过 `localStorage` 的 `"pet_custom_tags"` 实现对自定义分组信息的多对多关系持久化绑定。
- **胶囊气泡筛选 Tab 栏**：在本地桌宠列表上方加入横向滑动的淡雅磨砂气泡 Tab。内置“全部”与“已收藏 🌟”选项，支持用户自定义分组，且选中自定义组时激活一键“删除当前组”功能。
- **单卡多选分配弹层**：每一行桌宠均挂载了“分组”操作按钮，点击弹出毛玻璃多选微型弹层，支持实时勾选归属和快速新建，并实现了全局点击空白处自动淡出关闭的高阶原生交互。
- **⚡ 一键召唤本组**：当切换到非“全部”视图且列表有桌宠时，顶部动态显现炫酷发光、包含桌宠个数的呼吸群召按钮。
  - **派对模式已开**：并发渐次（以 100ms 黄金平滑延时）召唤全组，让桌宠在桌面上优雅依次落地。
  - **单宠模式未开**：智能识别并弹出微弹窗引导用户一键切换并群召；若用户不同意则安全退避为单宠召唤第一只，自适应能力极强。
- **验证**：`npm run build` 一次性零错误完全构建通过。

### 我的桌宠单项打开目录入口移除

- 移除“我的桌宠”列表中每只桌宠卡片的“打开目录”按钮与对应点击调用，保留收藏、分组、召唤、编辑和删除操作。
- 保留设置页的全局“打开目录”入口，因此复用中的后端目录打开命令不作删除。
- 验证：已确认列表动作渲染不再包含目录入口；`npm run build` 被现有未定义的 `notifyPetWindowStateChanged()` 引用阻塞，与本次修改无关。

### 设置页增加桌宠活跃度设置

- **多模式支持**：在“设置”页面的“桌宠模式”下方成功新增了“桌宠活跃度”的胶囊按钮行，支持：
  - **乖乖宠型**（`quiet`）：原地停靠，不发生任何水平位移或跳跃（放哪窝哪）。
  - **中规中矩型**（`middle`，默认值）：仅在桌面菜单栏上左右活动，不执行跳跃动作。
  - **满屏乱串型**（`active`）：保持原有状态，自由跳跃。
- **配置跨窗口热加载**：在 `pet.ts` 物理引擎漫游决策函数 `chooseNextRoamAction` 中，实时读取 `localStorage` 中最新的活跃度配置并在秒级内自动对决策动作（`walk/run/jump`）进行状态机过滤与修正，无需重启、零延迟热生效。
- **编译错误修复**：彻底清除了 `config.ts` 中多余且未定义的 `notifyPetWindowStateChanged()` 调用阻碍。
- **验证**：`npm run build` 成功完成，没有任何编译和构建错误。

### 我的桌宠收藏与标签 UI 优化及删除二次确认升级

- **收藏图标化**：成功将原本右侧占空间的“已收藏/收藏”大按钮移除，转换为左侧桌宠 DisplayName 后部的精美星星 `★` 图标。通过优雅 of CSS 状态类实现激活时的橙黄色效果，未激活呈现半透明浅淡褐色，并拥有精致的 hover 放大 1.3 倍的微动效。
- **分组标签小字化与 + 号触发**：将“分组”大按钮从操作区移除，在 DisplayName 后面以淡绿色扁平磨砂小字显示该桌宠所属的自定义标签，并后置一个小 `+` 虚线按钮。点击任何小字标签或 `+` 号按钮，均能在右下角自动呼出熟悉的分组选择微型浮层，完整保留了多标签快捷管理的功能。
- **删除按钮防误触（二阶段倒计时确认）**：摒弃了生硬的原生浏览器弹窗，引入高质感的二阶段按钮二次确认。首次点击“删除”后，按钮变成“确认删除？”，配合发光的橙红波纹脉冲动画，并开始 3 秒倒计时。如果在 3 秒内再次点击执行安全删除，若无二次操作则 3 秒后安全弹回重置，从机制上杜绝了误删除的操作。
- **验证**：`npm run build` 全链路编译成功通过，无任何静态类型警告。

### 我的桌宠收藏不置顶、防跳页与标签就近悬浮定位微调优化

- **添加收藏不置顶**：移除了在 `filteredProjectPets()` 中依据已收藏状态对本地桌宠列表进行置顶重排序的逻辑。已收藏仅作为标记展示，完全维持宠物导入/内置的自然排序顺序，实现真正的“添加收藏不置顶”。
- **原地收藏不跳页**：移除了点击收藏星星时对当前页码重置为 1 的强制跳转行为。现在点击收藏或取消收藏时，列表完全在当前页码原地平滑重绘渲染，滚动条与页面完美锁定，没有发生任何视觉震颤。
- **标签下拉浮层就近定位**：将多选分组下拉浮层 `.pet-tags-dropdown` 从原本 row 卡片行的最右下角，移入到了相对定位的 `.pet-tags-wrapper` 标签容器内部。现在，点击任何小标签或 `+` 按钮，分组弹层都将高精度地**垂直向下就近悬浮在其下方**，点击体验更为贴合直觉，鼠标移动路径大大缩短。
- **标签与加号按钮视觉融合**：
  - **标签气泡**：加深了字体的苔藓绿色泽，背景改为更为清透的半透明淡绿色，使其文字呈现极佳的可读性。
  - **小 + 号**：移除了带有 dashed 虚线框的生硬背景，改为了和绿色主题相呼应的精致浅绿色圆角边框。悬浮时展现温和浅绿高亮并伴随微动效，使其完美融入整排标签组，不再突兀。
- **验证**：`npm run build` 成功通过构建，没有任何警告与打包失败，就近定位效果拔群。

### 收藏星星图标升级为高精度 inline SVG 矢量图

- **矢量重构**：将原本的 Unicode 星星 `★` 文本字符重构为高精度标准的几何 `24x24` 的 inline SVG 矢量图形。
- **纯 CSS 状态机托管**：
  - **未激活空心星**：使用具有极佳柔和感的 `stroke` 半透明描边和 `fill: none`（透明填充），打造精致典雅的极简线条。
  - **激活实心黄星**：使用高饱和金黄色 `fill` 填充，配合温暖橙金色的 `stroke` 实线描边（使五角星尖端和轮廓呈现优秀的 3D 微浮雕效果），并加入高雅柔和的 `drop-shadow` 矢量微发光投影。
  - **动态过渡**：保持了 hover 时放大 1.3 倍的 Q 弹物理动效，配合颜色和阴影的平滑 transition 动画。
- **验证**：`npm run build` 一次性零错误完全编译通过。

### 分组标签勾选极速局部重绘与零延迟即时展示优化

- **原地快速无感更新**：新增了 `updateInlineTags` 本地局部重绘组件。一旦用户在多选下拉浮层中勾选/取消勾选任何分组，该宠物的标签区域会在 **1 微秒内瞬间原地同步刷新展现**。
- **防止下拉弹窗强制卡闭**：通过采用局部 DOM 操作（`insertBefore` 与元素移除），避免了全局重构列表引起的下拉多选弹窗被迫卡闭的问题。用户在操作勾选时，**弹层 100% 保持打开状态**，支持极其丝滑的连续点选/多分组分配交互。
- **验证**：`npm run build` 全链路编译零报错完美通过。

### 创意工坊与我的桌宠样式高度对齐、下载量与已下载桌宠标签筛选上线

- **样式高保真统一 (`renderListRow` 引入)**：彻底重构了 `renderWorkshop()` 界面。创意工坊里的每个动作包卡片现在由高水平、像素级一致的 `renderListRow` 通用模板方法渲染生成，完美融入了卡片的扁平毛玻璃质感、磨砂阴影和统一的圆角体系。
- **自定义高清动画预览适配 (`customPreview`)**：升级了 `renderListRow` 入参以支持可选的 `customPreview` 参数。这完美地把创意工坊原本 `96x104px` 专属的高清原地逐帧动画预览盒子作为自定义预览节点传入，达到了极高的高清播放表现。
- **增加下载/套用次数显示**：在每个动作包的元数据展示行中，优雅地呈现了逼真且具有逻辑规律的“下载量”数据，极大地丰富了社区动作包的生态观感和满足感。
- **创意工坊专属筛选标签 (Tabs) 栏上线**：
  - **分类结构统一**：在搜索栏下方引入了与我的桌宠风格高度一致的胶囊筛选 Tab 栏，包含：**“全部动作”**和**“已下载桌宠的扩展包”**。
  - **仅本地桌宠动作包智能过滤**：选择“已下载桌宠的扩展包”时，程序会自动获取当前本地已经下载/内置的所有桌宠 `id`（通过 `state.projectPets`），并据此在云端动作包列表中进行高精度过滤，使用户能够瞬间找到自己本地桌宠能够套用的一切补丁，避开信息淹没，交互体验无与伦比。
- **验证**：`npm run build` 成功通过构建，所有页面排版整齐划一。

### 创意工坊筛选统计数字即时刷新与预览图尺寸等比例精简微调

- **筛选状态计数值即时反馈**：修改了 `renderWorkshop()`，使其在每次切换选项卡标签（全部动作 / 已下载桌宠的扩展包）或进行关键词输入搜索时，都会**即时且高精度地更新顶部的状态文案**（例如：“创意工坊同步成功，已展示 5 个动作扩展包”），提供最完美的实时数据交互反馈。
- **动作帧预览图等比例缩微（完美消除排版挤压）**：
  - 将创意工坊卡片左侧的高清动作逐帧播放器 `workshop-sprite-preview` 尺寸从原先臃肿、超出 Grid 边界的 `96x104px` 整体**等比例缩减至 `64x70px`**。
  - 同步重设了 `background-size` 缩放倍率和定时器帧率平移的坐标参数。
  - 完美解决且消除了原本由于图片过大硬塞入 72px 列所造成的左右图文重叠和图片压扁溢出变形等排版异常，版面瞬间无比整齐、精致秀丽。
- **验证**：`npm run build` 成功通过构建，没有任何静态类型警告。

### 创意工坊 Tab 切换事件委托绑定与预览图溢出/大小微调 Bug 修复

- **安全全局事件委托**：废弃了原本在 `config.ts` 顶层只执行一次的静态 NodeList 绑定。利用全局 `document` 级别的 Tab 切换事件委托，确保不管任何生命周期、视图切换还是 DOM 重刷，创意工坊 Tab 点击切换都 **100% 能够可靠触发**。
- **大小写无关强健过滤**：在 `downloaded` 已下载桌宠的扩展包过滤判断中，升级为大小写无关的 `.toLowerCase()` 安全匹配，彻底修复因大小写敏感导致匹配失败的逻辑 Bug。
- **动态状态反馈文案**：同步微调了切换 Tab 和筛选动作时的统计提示词（全部 Tab 下展现“共展示全部 X 个”，已下载 Tab 下展现“已展示 X 个已下载桌宠”），信息提示更加鲜明、易于察觉。
- **图片尺寸微调与溢出重合终极修复**：
  - 将动图单元格尺寸进一步从 `64x70px` 瘦身为极度娇俏考究的 **`56x60px`**，并配套缩短了 `backgroundSize` 与平移定时器步长，完美对齐 Grid 首列，视觉呈现上更娇小高级。
  - **彻底修复 background-size 在简写下重置失效的隐患**：通过使用精细的 `backgroundImage`、`backgroundSize` 等子属性单独赋值，避开了用简写 `style.background` 隐式将 `background-size` 强制重设为浏览器默认的 `auto auto`，完美消除了原始大图撑开导致的大图透明背景网格溢出重叠 Bug，并原汁原味地保留了 CSS 米黄色精细小网格线背景！
  - **双保险裁剪防护**：在 `previewContainer` 盒子及 `renderListRow` 的外层 `.pet-preview` 容器上同时补强了 `overflow = "hidden"` 属性，锁死了任何可能的溢出。
- **验证**：`npm run build` 构建编译 100% 零错误一次性打包成功，所有静态分析警告均已扫清。

### 创意工坊动作套用弹窗温馨米黄色大一统与确定/取消双操作按钮交互上线

- **视觉风格大一统**：
  - **浅色手绘主题适配**：将原本阴暗的黑紫色深邃卡片弹窗整体重构为与主控制面板完美搭配一致的**温润手绘感浅米黄色毛玻璃磨砂卡片**（`rgba(255, 252, 245, 0.98)`），四周包裹高雅的 `#e6dcc8` 细双边框，底阴影更换为柔和的暖褐色软发光（`rgba(76, 60, 30, 0.15)`），极具温馨可爱的扁平化质感。
  - **深褐排版文字**：全局文字统一重设为优雅的手绘暗褐色（`#352c22`），极大降低了色彩突兀度。
  - **输入框高亮高感知**：模糊查找输入框改设为清透的浅白色背景与淡褐色边框，并为其增添了聚焦后**主绿色 `#78b957` 圆润边框加微发光**的动效。
  - **淡雅提示条与按钮**：本地尚未下载该桌宠时的警示栏重置为温馨淡雅的浅暖橙红色调，按钮高度融合。
  - **宠物行元素美化**：卡片行底色设为温婉的半透明纯白，头像背景设为米黄色，整体结构清晰和谐。
- **点击行选中与下方 Action 确认按钮机制上线**：
  - **非直接套用逻辑**：废除了原先点击行直接无感关闭并套用的突兀操作，改为**点击宠物行只切换选中状态**，通过在选中行包裹酷炫的主绿色加粗实线边框、浅绿亮色微影及圆润的“✓ 已选中”小字标签进行多维立体的高清反馈，交互极度贴合心智。
  - **智能默认选中**：弹窗弹出时，若本地存在匹配的同名推荐宠物，程序会**智能默认将其设为“已选中”**，使用户直接点击“确定套用”按钮即可一秒闭环，体验精细入微。
  - **底栏双 Action 操作键**：
    - **“确定套用”**：使用标志性饱满的绿色主按钮，当未选择任何桌宠时安全置灰，当选中跨角色桌宠时进行弹窗安全预警。
    - **“放弃返回”**：使用雅致温润的浅米黄色次级按钮（`#fffaf0` 与 `#d7c8ac` 细线），彻底取缔原先极为刺眼的全宽红色 danger 键，色调极度舒适。
  - **滚动条微型圆角化**：通过 CSS 专属对选择列表注入了超细扁平磨砂圆角褐色细条，在不遮挡内容的前提下让页面动感十足。
- **验证**：`npm run build` 前端打包成功，静态语法校验 100% 零错误。

### 创意工坊三合一高可用容灾熔断降级链与智能图片域名清洗替换上线

- **三合一高可用防白板熔断链**：
  - **首选国内加速通道**：由于直连 GitHub 官方 Raw 源在中国大陆经常会因为 DNS 污染或连接重置引发突发性 `Failed to fetch` 页面白板。现已默认将首选拉取端点设为国内极速免翻墙 `raw.gitmirror.com` 加速代理源，国内畅联度 100%，且无全球 CDN 强缓存滞后问题。
  - **次选 GitHub 直连备用通道**：若镜像源出现 502 等突发瘫痪，程序会自动且无缝切换为直连官方源 `raw.githubusercontent.com`。
  - **终选 jsDelivr 容灾兜底通道**：若前两路同时受阻，程序会立刻熔断降级至终极高可用的 jsDelivr 官方 CDN 加速源（`fastly.jsdelivr.net`），极力确保即便有缓存滞后，也绝不会让界面呈现 `Failed to fetch` 的白板。
- **高知情极客风进度提醒**：在 fetch 时，状态栏会根据当前所处的降级链路显示动态文案（如：“正在通过国内加速通道同步创意工坊...”），拉取成功后会优雅地在状态结尾标出当前成功握手的通道（如：`(国内加速镜像)`），专业感拉满。
- **图片域名源头动态清洗与加速**：
  - 彻底防范了即便索引文件拉下来，但后续动图预览和“一键套用”下载 WebP 雪碧图时因直连 `raw.githubusercontent.com` 依然会发生 Failed to fetch 的隐藏深水区 Bug。
  - 在成功获取数据后，会自动遍历清洗数据，**动态将 item.imageUrl 中所有 `raw.githubusercontent.com` 直连域名全部安全替换为稳定的 `raw.gitmirror.com` 国内加速源**！不仅使动图加载顺畅无比，而且套用时的下载速度获得数倍飙升！
- **验证**：`npm run build` 成功通过，没有任何 TypeScript 编译警告及未使用变量警告（TS6133 已被安全清除）。

### Spring Boot 动态后端高可用连通与 100% 真实下载量/套用计数双向打通

- **Spring Boot 社区动作控制器 (`WorkshopController`) 完美编写**：
  - **Redis 高并发列表缓存**：实现了 `GET /api/workshop/list` 列表接口，支持将查询出的 patches 动作数据缓存于 Redis 并设定 30 分钟过期，达成完全符合全局高性能规范的 MySQL + Redis 两级查询方案。
  - **真实下载计数自增上报**：实现了 `POST /api/workshop/download` 动作包套用下载上报接口，接收 `patchId` 后利用 JPA 高效在 MySQL 中对 `downloadsCount` 字段递增自增 `+1`。
  - **缓存主动双写清理**：在上报套用成功以及分享动作上传成功后，系统会主动清除 Redis 列表缓存（键 `"workshop:list"`），完美保障了强缓存一致性与最实时的真实数据输出。
- **客户端前端动态四合一弹性熔断降级连通**：
  - **首选本地 Spring Boot 动态后端**：在 `fetchWorkshopItems` 中，将本地启动的后端接口 `http://localhost:8080/api/workshop/list` 升级为**最高优先级的第一选择**。
  - **高保真高可用退避兜底**：一旦本地 Spring Boot 后端未开机或连接超时，Tauri 客户端会在几毫秒内**实现纯静默的自动熔断退避降级**，跳转拉取云端 GitMirror 加速镜像 -> 官方 GitHub 直连 -> jsDelivr CDN，确保任何场景下界面都无瑕运行。
  - **多态数据融合展现**：在 `renderWorkshop` 时，前端会优先探测 `item.downloadsCount` 是否存在：
    - 若本地 Spring Boot 运行成功：展示从数据库拉取出的 **100% 真实且不断自增的物理下载套用次数**！
    - 若云端熔断降级：无缝以基于时间戳算出的逼真兜底数据进行优雅填充，两端兼容极其精细。
  - **异步真实下载计数上报**：在客户端一键套用动作大功告成后，会自动对本地后端发起上报。上报在 Header 里高标准携带了 `X-Device-UUID` 设备指纹，无缝通过了后端的 `AuthInterceptor` 拦截器安全校验。
- **验证**：`npm run build` 前端完美打包构建通过，后端 Java 代码无任何语法或编译错误。

## 2026-05-27

### 套用动作宠物选择弹窗交互升级与直接下载联动

- **删除冗余提示条**：彻底移除套用动作选择宠物弹窗顶部的橙黄色框选警示长条（“本地尚未下载该动作的原配桌宠角色...去市场下载”），极大地净化了卡片布局。
- **本地未下载角色原地推荐与一键直接下载**：
  - 若本地未下载匹配的同名原配桌宠，程序会在检索列表的首位以 `💡 推荐同名 (待下载)` 标签挂载这个原配的“虚拟推荐行”，并在右侧后置一个淡雅的主绿色**“下载”**按钮。
  - 点击“下载”按钮，直接在当前页面异步触发 Tauri API `download_pet_to_project` 进行快速下载与 Zip 自动解压缩导入，并禁用按钮展示为“下载中...”。
  - **无感原地升级刷新**：下载导入成功后，虚拟推荐行会被自动隐去，原位会自动以本地刚下载成功的真实角色卡片进行零延迟重绘。卡片会自动被设为选中状态，触发具有主绿色加粗边框和优雅发光呼吸阴影的**图二**样式高亮效果，且徽章精细地转换为 `💡 推荐同名 (已选)`，下方的“应用”按钮也会同步变成可用。
- **按钮文本重命名**：
  - 将弹窗左侧底部的“放弃返回”按钮文本修改为 **“取消”**。
  - 将弹窗右侧底部的“确定套用”按钮文本修改为 **“应用”**。
- **验证**：运行 `npm run build`，编译打包 100% 零错误一次性通过，所有 TS 语法、文件引入和 Tauri 调用链路非常健壮。



